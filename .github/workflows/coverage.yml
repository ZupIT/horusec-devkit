name: Coverage
on:
  push:
    branches:
      - master
      - develop
  pull_request:
jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v1
      - name: coverage
        run: make coverage
