name: Lint
on:
  push:
    branches:
      - master
      - develop
  pull_request:
    branches-ignore:
      - master
      - develop
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: lint
        run: make lint
